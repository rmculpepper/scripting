(module info (lib "infotab.ss" "setup")
  (define name "scripting")
  (define compile-omit-files '())
  (define blurb '("Scripting is a library of procedures useful for writing "
		  "mzscheme scripts, especially those which execute "
		  "external commands and manipulate the filesystem."))
  (define primary-file "scripting.ss")
  (define version "1.1")
  (define categories '(io misc))
  (define doc.txt "doc.txt"))
